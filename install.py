import sys
import os
import subprocess
import glob
import platform

BASE_DIR = os.path.dirname(os.path.dirname(__file__))

# try:
#
#     subprocess.call(['/usr/bin/python','install','-r','requirements.txt'])
# except Exception:
#     _OS = platform.
